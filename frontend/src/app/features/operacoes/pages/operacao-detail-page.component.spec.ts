import { TestBed } from '@angular/core/testing';import { ActivatedRoute,provideRouter,Router } from '@angular/router';import { of,throwError } from 'rxjs';import { NormalizedHttpError } from '../../../core/errors/normalized-http-error';import { OperacaoResponse } from '../models/operacao';import { OperacoesService } from '../operacoes.service';import { OperacaoDetailPageComponent } from './operacao-detail-page.component';
const item:OperacaoResponse={id:7,carteiraId:1,ticker:'AAPL',mercado:'EUA',corretoraId:null,tipo:'COMPRA',quantidade:'1234567890123.123456',precoUnitario:'0.000001',dataOperacao:'2026-09-01',ordemNoDia:1,valorTotal:'12345678901234567890.123456789012'};
describe('OperacaoDetailPageComponent',()=>{afterEach(()=>TestBed.resetTestingModule());async function create(info?:OperacaoResponse,failure?:NormalizedHttpError){const service={buscarPorId:vi.fn().mockReturnValue(failure?throwError(()=>failure):of(item))};await TestBed.configureTestingModule({imports:[OperacaoDetailPageComponent],providers:[provideRouter([]),{provide:ActivatedRoute,useValue:{snapshot:{paramMap:{get:()=> '7'}}}},{provide:OperacoesService,useValue:service}]}).compileComponents();const router=TestBed.inject(Router);vi.spyOn(router,'currentNavigation').mockReturnValue(info?({extras:{info:{operacao:info,origin:'/carteiras/1'}}} as never):null);const fixture=TestBed.createComponent(OperacaoDetailPageComponent);fixture.detectChanges();return{fixture,service};}
it('usa DTO transitório completo sem GET e exibe preço e total autoritativos',async()=>{const{fixture,service}=await create(item);expect(service.buscarPorId).not.toHaveBeenCalled();expect(fixture.nativeElement.textContent).toContain('0,000001');expect(fixture.nativeElement.textContent).toContain('12.345.678.901.234.567.890,123456789012');expect(fixture.nativeElement.textContent).toContain('Sem corretora');expect(fixture.nativeElement.textContent).not.toMatch(/Editar|Excluir/);});
it('faz GET no reload',async()=>{const{service}=await create();expect(service.buscarPorId).toHaveBeenCalledWith(7);});
it('apresenta 404 dedicado',async()=>{const error={status:404,message:'Ausente',details:{},code:null} as unknown as NormalizedHttpError;const{fixture}=await create(undefined,error);expect(fixture.nativeElement.textContent).toContain('Operação não encontrada');expect(fixture.nativeElement.textContent).toContain('Voltar para operações');});});
