import { ChangeDetectionStrategy, Component, DestroyRef, inject, signal } from '@angular/core';
import { takeUntilDestroyed } from '@angular/core/rxjs-interop';
import { MatButtonModule } from '@angular/material/button';import { MatCardModule } from '@angular/material/card';import { MatProgressSpinnerModule } from '@angular/material/progress-spinner';import { ActivatedRoute,Router,RouterLink } from '@angular/router';import { finalize } from 'rxjs';
import { NormalizedHttpError } from '../../../core/errors/normalized-http-error';import { FeedbackAlertComponent } from '../../../shared/feedback-alert/feedback-alert.component';import { PageHeaderComponent } from '../../../shared/page-header/page-header.component';import { StickyBackComponent } from '../../../shared/sticky-back/sticky-back.component';import { OperacaoResponse } from '../models/operacao';import { formatCivilDate,formatDecimal } from '../operacao-validators';import { OperacoesService } from '../operacoes.service';
@Component({selector:'app-operacao-detail-page',imports:[FeedbackAlertComponent,MatButtonModule,MatCardModule,MatProgressSpinnerModule,PageHeaderComponent,RouterLink,StickyBackComponent],template:`<section class="app-page" aria-labelledby="operation-detail-title"><app-sticky-back [route]="origin()" label="Voltar"/><app-page-header headingId="operation-detail-title" eyebrow="Detalhe da operação" [title]="item() ? item()!.tipo + ' · ' + item()!.ticker : notFound() ? 'Operação não encontrada' : 'Operação'" description="Dados autoritativos registrados pelo backend."/>@if(error()){<app-feedback-alert variant="error" [message]="error()!.message" [details]="error()!.details"/>}@if(loading()){<div class="app-state" role="status" aria-live="polite"><mat-spinner diameter="36"/>Carregando operação…</div>}@else if(error()){<div class="app-state">@if(notFound()){<a mat-stroked-button routerLink="/operacoes">Voltar para operações</a>}@else{<button mat-stroked-button type="button" (click)="load()">Tentar novamente</button>}</div>}@else if(item();as op){<mat-card class="section-card" appearance="outlined"><mat-card-content><dl class="data-list"><div><dt>Identificador</dt><dd>{{op.id}}</dd></div><div><dt>Tipo</dt><dd>{{op.tipo}}</dd></div><div><dt>Carteira</dt><dd>#{{op.carteiraId}}</dd></div><div><dt>Ação</dt><dd>{{op.ticker}} · {{op.mercado}}</dd></div><div><dt>Corretora</dt><dd>{{op.corretoraId===null||op.corretoraId===undefined?'Sem corretora':'#'+op.corretoraId}}</dd></div><div><dt>Quantidade</dt><dd>{{decimal(op.quantidade)}}</dd></div><div><dt>Preço negociado</dt><dd>{{decimal(op.precoUnitario)}} {{currency(op)}}</dd></div><div><dt>Valor total</dt><dd>{{decimal(op.valorTotal)}} {{currency(op)}}</dd></div><div><dt>Data da operação</dt><dd>{{date(op.dataOperacao)}}</dd></div><div><dt>Ordem no dia</dt><dd>{{op.ordemNoDia}}</dd></div></dl></mat-card-content></mat-card>}</section>`,styles:[`.section-card{max-width:52rem}.section-card mat-card-content{padding:1.5rem}.data-list dd{overflow-wrap:anywhere}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class OperacaoDetailPageComponent{private readonly service=inject(OperacoesService);private readonly route=inject(ActivatedRoute);private readonly router=inject(Router);private readonly destroyRef=inject(DestroyRef);private readonly id=Number(this.route.snapshot.paramMap.get('id'));protected readonly item=signal<OperacaoResponse|null>(this.navigationItem());protected readonly origin=signal(this.navigationOrigin());protected readonly loading=signal(this.item()===null);protected readonly error=signal<NormalizedHttpError|null>(null);protected readonly notFound=signal(false);protected readonly date=formatCivilDate;protected readonly decimal=formatDecimal;constructor(){if(!this.item())this.load();}protected currency(op:OperacaoResponse):string{return op.moeda??(op.mercado==='BRASIL'?'BRL':'USD');}protected load():void{this.loading.set(true);this.error.set(null);this.notFound.set(false);this.service.buscarPorId(this.id).pipe(finalize(()=>this.loading.set(false)),takeUntilDestroyed(this.destroyRef)).subscribe({next:v=>this.item.set(v),error:(e:NormalizedHttpError)=>{this.notFound.set(e.status===404);this.error.set(e);}});}private navigationInfo():{operacao?:OperacaoResponse;origin?:string}|undefined{return this.router.currentNavigation()?.extras.info as {operacao?:OperacaoResponse;origin?:string}|undefined;}private navigationItem():OperacaoResponse|null{const c=this.navigationInfo()?.operacao;return c&&c.id===this.id&&typeof c.valorTotal==='string'&&typeof c.quantidade==='string'&&typeof c.precoUnitario==='string'?c:null;}private navigationOrigin():string{return this.navigationInfo()?.origin??'/operacoes';}}
